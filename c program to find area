#include<stdio.h>

int area(int x);

int main() {
    int a, b;
    printf("Enter the value of a: ");
    scanf("%d", &a);
    b = area(a);
    printf("\nArea of the square is %d", b);
    return 0;
}

int area(int x) {
    return x * x;
}